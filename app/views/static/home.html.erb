<div class="container mx-auto px-5 py-5">
  <div class="flex justify-center items-center">
    <%= image_tag "banner5.png", class: "h-[270px] w-[720px] object-scale-down flex-1" %>
    <div class="text-center text-2xl flex-1 tracking-wider">
      <p>インク沼のための</p>
      <p>万年筆・ガラスペン用インクレビューサービス</p>
    </div>
  </div>
  <h1 class="text-5xl font-bold text-center mb-10 tracking-wider">レビューを見る</h1>
  <div class="grid grid-cols-2 gap-4">
    <% if @reviews.present? %>
      <%= render partial: 'reviews/review', collection: @reviews %>
    <% else %>
      <div class="tracking-wider">レビューがありません。</div>
    <% end %>
  </div>
</div>